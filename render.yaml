services:
  - type: web
    name: donatello
    repo: https://github.com/DEV-AusA/donatello-test
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn donatello.wsgi:application --bind 0.0.0.0:5000